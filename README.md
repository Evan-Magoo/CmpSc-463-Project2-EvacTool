Download all files (including images) and place into a python project. The Backend folder should be left as is. To run the program, run the GUI.py file!

Node/Location selection is done by clicking on the node with the mouse.
